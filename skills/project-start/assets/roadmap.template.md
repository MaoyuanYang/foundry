# Roadmap Template

<!-- ==================================================================== -->
<!-- File: specs/ROADMAP.md                                               -->
<!-- ==================================================================== -->

# Roadmap

<One paragraph on the current direction and the shortest credible path to a useful
product.>

## Features

| ID    | Feature          | Status    | Spec                     |
| ----- | ---------------- | --------- | ------------------------ |
| F001  | <name>           | Next      | specs/F001-<slug>.md     |
| F002  | <name>           | Draft     | specs/F002-<slug>.md     |

In GitHub mode, add an `Issue` column linking each feature's issue.

Status values:

- **Draft** — described in a spec, not yet scheduled.
- **Next** — selected as the next feature to build (`project-dev` picks this up; keep
  exactly one Next at a time).
- **In Progress** — currently being implemented by `project-dev`.
- **In Review** — GitHub mode only: the pull request is open, awaiting CI and review.
- **Done** — implemented and verified.

## Tracking

<GitHub mode only — delete this section when the project works locally.>

- Collaboration: GitHub — enabled <date>  (or: local — the user's decision at project
  initialization; later skills read this instead of asking again)
- Project board: <url>
- Current wave milestone: <name>

## Notes

<Dependencies between features, sequencing rationale, and ideas deliberately deferred.>
